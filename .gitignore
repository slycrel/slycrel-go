# Binary (anchored to repo root — don't clobber cmd/slycrel/)
/slycrel

# Game state (player data, not source)
data/state/

# macOS
.DS_Store

# IDE
.idea/
.vscode/
*.swp
*~

# Legacy archives (too large for git)
orig/**/*.zip
orig/**/*.sit

# SVN metadata from original source
orig/**/.svn/

# Claude Code internals
.claude/
state/
